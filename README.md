# Distance Tracker App + Aether Nexus Blueprint

This repository now contains:
- Existing distance-tracker web app (`public/`, `server.js`)
- A production-scale architecture blueprint for an AI-native unified super-platform (`platform/`)

Start existing app:
```bash
npm install
npm run dev
```

Explore blueprint:
- `platform/README.md`
- `platform/docs/`
- `platform/contracts/`
- `platform/infra/`
